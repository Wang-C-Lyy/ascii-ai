# ascii-ai
这是一个基于多模态智能体的 AI 项目，核心目标是让模型同时理解图片和文本信息，并进行联合建模与推理。项目采用 bert-base-chinese 作为文本编码器，用自定义的 ImageEncoder 提取图像特征，再通过 MultimodalAgent 将视觉和语言表达对齐与融合，以支持下游任务（如多模态问答、描述生成或检索等）。训练过程中通过统一的配置文件 config 管理超参数，设置随机种子保证实验可复现。整体框架清晰，便于扩展新的模型结构和多模态任务，是一个面向中文场景的多模态深度学习实验平台。
